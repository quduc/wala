import React from "react";
import { Body } from "@components/index";
import { useEffect } from "react";
const PlaylistScreen = () => {
  useEffect(() => {}, []);

  return <Body ph={16} pt={45} loading={false}></Body>;
};

export default PlaylistScreen;
